class server:
    def __init__(self, arch, cpu, ram, disk):
        self. arch = arch
        self.cpu = cpu      // number of cores
        self.ram = ram
        self.disk = disk
        self.nic = nic  

    def set_bootorder():
    def configure_raid():
    def configure_storage_layout():
    def kernel_params():
    def add_disk():
    def upgrade_nic():
    def deploy(os):

class pdu:
    def __init__(self, capacity, size, power):
        s = list(server())
        self.capacity = len(s)  // number of servers in a rack
        self.size = size  //size of each unit e.g 1U, 2U
        self.power = power  //power param


